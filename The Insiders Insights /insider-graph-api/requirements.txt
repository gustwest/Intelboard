fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.2
pydantic-settings==2.5.2
google-cloud-firestore==2.19.0
google-cloud-storage==2.18.2
google-cloud-scheduler==2.13.5
httpx==0.27.2
python-multipart==0.0.9
# langchain 0.3.7 kräver numpy<2; opinnad openai drar numpy>=2 → olösbart i ren
# build. Pinna numpy<2 så pip väljer en openai som funkar (verifierat i ren venv).
numpy>=1.26,<2
langchain==0.3.7
langchain-openai==0.2.5
# Pinna openai till en version utan hårt numpy>=2-krav. Dockers pip (25.0.1) hittar
# den inte själv (backtrackar nedåt från 1.68), så vi pekar ut den. Verifierat med
# pip 25.0.1 --dry-run att hela trädet löser med numpy<2.
openai==1.109.1
langchain-google-genai==2.0.4
langchain-google-vertexai==2.0.7
# ChatAnthropicVertex (langchain_google_vertexai.model_garden) kräver anthropic-
# SDK:n vid runtime även om vi går via Vertex Model Garden — utan den failar
# Claude-proben med "No module named 'anthropic'" vid init. langchain-google-
# vertexai pinnar inte den som hård dep, så vi måste lägga in den explicit.
#
# 2026-06-04: bumpad 0.40.0 → 0.104.1. Den gamla versionen byggde FEL endpoint för
# location="global": "https://global-aiplatform.googleapis.com/..." (ogiltig värd →
# 404 "generic Google page"). 0.104.1 bygger korrekt "https://aiplatform.googleapis.com/
# v1/" med location=global i path:en.
anthropic==0.104.1
# Claude-proben bytte 2026-06-04 från Vertex Model Garden (quota=0) till första-parts
# Anthropic API via langchain_anthropic.ChatAnthropic. Pinnad till 0.3.13 — verifierat
# i clean-room (tom venv, pip dry-run) att den löser mot HELA requirements.txt med
# Dockers strikta resolver. 0.3.22 löste lokalt men inte i Docker (krockade med
# anthropic==0.104.1-pinningen). 1.x-linjen drar in langchain-core 1.x och bryter
# langchain-google-vertexai 2.0.7 (kräver langchain-core <0.4).
langchain-anthropic==0.3.13
trafilatura==2.0.0
pypdf==6.12.1
sendgrid==6.11.0
openpyxl==3.1.5
xlrd==2.0.1
defusedxml==0.7.1
# IANA-tidszondatabasen. python:3.12-slim saknar OS-zonfilerna, så zoneinfo.ZoneInfo
# ("Europe/Stockholm") i services/clock.py kräver detta paket för att inte kasta
# ZoneInfoNotFoundError i prod.
tzdata==2025.2
slowapi==0.1.9
